# API Configuration
# In development: Set this to your local backend URL (optional, will use proxy if not set)
# In production: Leave unset to use relative path /api (served by same domain)
# VITE_API_URL=http://localhost:5000/api
