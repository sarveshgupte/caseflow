# Visual Guide: Case Identifier Fix

## Problem: Inconsistent Navigation

```
┌─────────────────────────────────────────────────────────────────┐
│                        BEFORE THIS PR                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  Frontend Navigation:                                            │
│  ┌────────────────┐                                             │
│  │ WorklistPage   │ navigate(`/cases/${caseName}`)              │
│  └────────────────┘        ↓                                     │
│           ❌ Uses "case2026010900001" format                     │
│                                                                   │
│  ┌────────────────┐                                             │
│  │ DashboardPage  │ navigate(`/cases/${caseName}`)              │
│  └────────────────┘        ↓                                     │
│           ❌ Uses "case2026010900001" format                     │
│                                                                   │
│  ┌────────────────┐                                             │
│  │ ReportsTable   │ onClick(caseName)                           │
│  └────────────────┘        ↓                                     │
│           ❌ Uses "case2026010900001" format                     │
│                                                                   │
│  Backend Expects:                                                │
│  ┌────────────────┐                                             │
│  │ case.controller│ findOne({ caseId: params.caseId })          │
│  └────────────────┘        ↑                                     │
│           ✅ Expects "CASE-20260109-00001" format                │
│                                                                   │
│  Result: ❌ MISMATCH → "Case not found" errors                   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

```
┌─────────────────────────────────────────────────────────────────┐
│                         AFTER THIS PR                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  Frontend Navigation:                                            │
│  ┌────────────────┐                                             │
│  │ WorklistPage   │ navigate(`/cases/${caseId}`)                │
│  └────────────────┘        ↓                                     │
│           ✅ Uses "CASE-20260109-00001" format                   │
│                                                                   │
│  ┌────────────────┐                                             │
│  │ DashboardPage  │ navigate(`/cases/${caseId}`)                │
│  └────────────────┘        ↓                                     │
│           ✅ Uses "CASE-20260109-00001" format                   │
│                                                                   │
│  ┌────────────────┐                                             │
│  │ ReportsTable   │ onClick(caseId)                             │
│  └────────────────┘        ↓                                     │
│           ✅ Uses "CASE-20260109-00001" format                   │
│                                                                   │
│  Backend Expects:                                                │
│  ┌────────────────┐                                             │
│  │ case.controller│ findOne({ caseId: params.caseId })          │
│  └────────────────┘        ↑                                     │
│           ✅ Expects "CASE-20260109-00001" format                │
│                                                                   │
│  Result: ✅ MATCH → All navigation works perfectly               │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Pull Assignment Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    ATOMIC PULL ASSIGNMENT                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  User Action:                                                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ User clicks "Pull" on case CASE-20260109-00001             │ │
│  └────────────────────────────────────────────────────────────┘ │
│                            ↓                                     │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ pullCase endpoint → caseAssignment.assignCaseToUser()      │ │
│  └────────────────────────────────────────────────────────────┘ │
│                            ↓                                     │
│  Atomic Update (findOneAndUpdate):                              │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 1. assignedTo        = "X123456"  ✅                        │ │
│  │ 2. status            = "OPEN"     ✅                        │ │
│  │ 3. queueType         = "PERSONAL" ✅                        │ │
│  │ 4. assignedAt        = now()      ✅                        │ │
│  │ 5. lastActionByXID   = "X123456"  ✅ NEW!                   │ │
│  │ 6. lastActionAt      = now()      ✅ NEW!                   │ │
│  └────────────────────────────────────────────────────────────┘ │
│                            ↓                                     │
│  Audit Trail:                                                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ CaseAudit.create({ actionType: 'CASE_ASSIGNED', ... })    │ │
│  │ CaseHistory.create({ actionType: 'CASE_ASSIGNED', ... })  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                            ↓                                     │
│  Results:                                                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ ✅ Case removed from Global Worklist                        │ │
│  │ ✅ Case added to My Worklist                                │ │
│  │ ✅ Dashboard "My Open Cases" count +1                       │ │
│  │ ✅ Opens in EDIT mode (not view-only)                       │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Worklist Query Consistency

```
┌─────────────────────────────────────────────────────────────────┐
│                    QUERY CONSISTENCY                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  My Worklist Query:                                              │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Case.find({                                                 │ │
│  │   assignedTo: user.xID,     // X123456                     │ │
│  │   status: "OPEN"             // Only OPEN cases            │ │
│  │ })                                                          │ │
│  └────────────────────────────────────────────────────────────┘ │
│                            ↓                                     │
│  Dashboard "My Open Cases" Count:                                │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ worklistService.getEmployeeWorklist(user.email)            │ │
│  │ → Uses EXACT SAME QUERY as My Worklist                     │ │
│  │ → Count = worklist.data.length                             │ │
│  └────────────────────────────────────────────────────────────┘ │
│                            ↓                                     │
│  Result: ✅ 100% CONSISTENCY                                     │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ My Worklist shows:    5 cases                               │ │
│  │ Dashboard count:      5 cases                               │ │
│  │ ALWAYS MATCH!                                               │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  Global Worklist Query:                                          │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Case.find({                                                 │ │
│  │   status: "UNASSIGNED"       // Only unassigned cases      │ │
│  │ })                                                          │ │
│  └────────────────────────────────────────────────────────────┘ │
│                            ↓                                     │
│  Result: ✅ Shows only truly unassigned cases                    │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Case View Mode Determination

```
┌─────────────────────────────────────────────────────────────────┐
│                    VIEW MODE LOGIC                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  Step 1: Fetch Case (NO OWNERSHIP FILTER)                       │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Case.findOne({ caseId: "CASE-20260109-00001" })            │ │
│  │ → Fetches case for ANY authenticated user                  │ │
│  │ → Does NOT filter by assignedTo                            │ │
│  └────────────────────────────────────────────────────────────┘ │
│                            ↓                                     │
│  Step 2: Determine View Mode (AFTER FETCH)                      │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ if (case.assignedTo === currentUser.xID) {                 │ │
│  │   viewMode = "EDIT"          ✅ User owns the case         │ │
│  │ } else {                                                    │ │
│  │   viewMode = "VIEW_ONLY"     ℹ️  User doesn't own case     │ │
│  │ }                                                           │ │
│  └────────────────────────────────────────────────────────────┘ │
│                            ↓                                     │
│  Results:                                                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ ✅ Any user can VIEW any valid case                         │ │
│  │ ✅ Only owner can EDIT case                                 │ │
│  │ ✅ No "Case not found" for valid cases                      │ │
│  │ ✅ Clear "View-Only Mode" indicator shown                   │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Post-Create Workflow

```
┌─────────────────────────────────────────────────────────────────┐
│                    CREATE CASE WORKFLOW                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  BEFORE:                                                         │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 1. Fill form and submit                                     │ │
│  │ 2. ❌ Auto-redirect to /cases/case2026010900001             │ │
│  │ 3. ❌ User stuck on case detail page                        │ │
│  │ 4. ❌ Must manually go back to create another              │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  AFTER:                                                          │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 1. Fill form and submit                                     │ │
│  │                                                             │ │
│  │ 2. ✅ Success message appears:                              │ │
│  │    ┌─────────────────────────────────────────────────┐    │ │
│  │    │ ✅ Case Created Successfully!                    │    │ │
│  │    │ Case CASE-20260109-00001 created and moved      │    │ │
│  │    │ to Global Worklist.                              │    │ │
│  │    │                                                   │    │ │
│  │    │ [Go to Global Worklist] [Create Another Case]   │    │ │
│  │    └─────────────────────────────────────────────────┘    │ │
│  │                                                             │ │
│  │ 3. ✅ Two clear options:                                    │ │
│  │    Option A: Go to Global Worklist → Pull the case        │ │
│  │    Option B: Create Another Case → Form resets             │ │
│  │                                                             │ │
│  │ 4. ✅ Efficient workflow for multiple case creation        │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Model Invariants

```
┌─────────────────────────────────────────────────────────────────┐
│                    DATA MODEL RULES                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  Rule 1: PERSONAL queue must have assignedTo                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ if (queueType === "PERSONAL") {                             │ │
│  │   REQUIRE: assignedTo !== null                              │ │
│  │   ✅ Enforced by atomic pull operation                      │ │
│  │ }                                                            │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  Rule 2: GLOBAL queue must NOT have assignedTo                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ if (queueType === "GLOBAL") {                               │ │
│  │   REQUIRE: assignedTo === null                              │ │
│  │   ✅ Enforced by default values on create                   │ │
│  │ }                                                            │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  Rule 3: Cannot transition to OPEN without owner                │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ if (status === "OPEN") {                                    │ │
│  │   REQUIRE: assignedTo !== null                              │ │
│  │   ✅ Enforced by atomic pull operation                      │ │
│  │ }                                                            │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  Rule 4: xID is mandatory for all actions                       │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ All operations require: user.xID (e.g., "X123456")         │ │
│  │ ✅ Assignment service validates xID exists                  │ │
│  │ ✅ All queries use xID, never email                         │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Field Reference

```
┌─────────────────────────────────────────────────────────────────┐
│                    CASE IDENTIFIER FIELDS                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  caseId (CANONICAL - USE THIS)                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Format:    CASE-YYYYMMDD-XXXXX                              │ │
│  │ Example:   CASE-20260109-00001                              │ │
│  │ Use For:   ✅ URLs, routes, queries, navigation             │ │
│  │ Storage:   MongoDB, indexed, unique                         │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  caseName (DISPLAY ONLY)                                         │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Format:    caseYYYYMMDDxxxxx                                │ │
│  │ Example:   case2026010900001                                │ │
│  │ Use For:   ✅ Display in tables/lists only                  │ │
│  │ NOT For:   ❌ URLs, routes, queries, navigation             │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  displayCaseId (DOES NOT EXIST)                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ ❌ This field does NOT exist in the codebase                │ │
│  │ ❌ Never use for any purpose                                │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Testing Checklist

```
✅ Case navigation from all pages uses caseId
✅ Pull case → appears in My Worklist
✅ Pull case → Dashboard count increments
✅ Pulled case opens in EDIT mode
✅ View-only mode for non-assigned cases
✅ Post-create shows success message
✅ Post-create action buttons work
✅ Bulk pull operations work
✅ Concurrent pull handling (race condition)
✅ Global Worklist shows only UNASSIGNED
✅ Dashboard count matches worklist count
✅ No "Case not found" errors
```

---

## Success!

All issues fixed. All acceptance criteria met. Ready for deployment! 🚀
